View:

CREATE VIEW products_above_5000 AS
SELECT *FROM product WHERE price>5000;



Delete Trigger:

CREATE TRIGGER manufacturer_Delete 
AFTER DELETE 
ON manufacturer FOR EACH ROW 
DELETE FROM product WHERE product.manufacturer_id=old.id;



Stored procedure:

CREATE PROCEDURE `add_manufacturer`(IN `nm` VARCHAR(50), IN `ad` VARCHAR(100), IN `con` VARCHAR(50)) NOT DETERMINISTIC CONTAINS SQL SQL SECURITY DEFINER INSERT INTO manufacturer(id, name, address, contact_no) VALUES (NULL, nm, ad, con);