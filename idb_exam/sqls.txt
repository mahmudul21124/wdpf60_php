Create Procedure:

CREATE PROCEDURE `manufacturer_add`(IN `name` VARCHAR(50), IN `address` VARCHAR(100), IN `contact` VARCHAR(50)) NOT DETERMINISTIC CONTAINS SQL SQL SECURITY DEFINER INSERT INTO manufacturer (id, name, address, contact_no) VALUES (NULL, name, address, contact);


Create Trigger:

CREATE TRIGGER `delete_manufacturer` 
AFTER DELETE ON `manufacturer` 
FOR EACH ROW 
DELETE FROM product WHERE product.manufacturer_id = old.id


Create View:

CREATE VIEW price_above_5000 AS 
SELECT * FROM product WHERE price > 5000;